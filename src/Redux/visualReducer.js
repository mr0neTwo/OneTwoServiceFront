const initialState = {
   statusMenuVisible : {},
   
   
   
}

export const visualReducer = (state = initialState, action) => {
   switch (action.type){

      // Подгрузка данных =============================================================

     

      
      

      
      default: return state
   }
   
}
